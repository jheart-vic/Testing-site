function stringLength(string) {
  let stringlen = string.length;
  // if(stringlen >= 1  && stringlen < 10){
  //   return "good job"
  // } else if(stringlen < 1  && stringlen > 10)  {
  //   return "stringlength must be at least 1 and less than 10"
  // }
  return stringlen
}
  module.exports = stringLength;